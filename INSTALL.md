# D-Note 설치 안내

오프라인 전용 · 암호화 데스크톱 노트 앱 (Windows x64)

## 1. 다운로드

[**Releases 페이지**](https://github.com/homebox78/desknote/releases)에서 최신
`D-Note_<버전>_x64-setup.exe` 파일을 내려받습니다.

## 2. 설치

1. 받은 `D-Note_*_x64-setup.exe` 를 더블클릭합니다.
2. Windows SmartScreen 경고가 뜨면(서명되지 않은 설치 파일이라 정상입니다)
   **추가 정보 → 실행**을 누릅니다.
3. 관리자 권한(UAC) 허용을 묻습니다 — 시스템 전체 설치(per-machine)라 필요합니다.
4. EULA 동의 후 안내에 따라 설치를 마칩니다.
5. 시작 메뉴 또는 바탕화면의 **D-Note** 로 실행합니다.

> WebView2 런타임이 없는 PC라면 설치 중 자동으로 내려받아 설치합니다(이 단계에만
> 인터넷이 필요). 최신 Windows 10/11에는 대부분 기본 포함되어 있습니다.

## 3. 첫 실행

- **처음 설치**: 마스터 비밀번호를 설정합니다. 이 비밀번호로 데이터베이스 전체가
  암호화됩니다. **분실 시 복구할 수 없으니** 안전하게 보관하세요.
- **기존 사용자 (업데이트)**: 기존 비밀번호로 잠금을 해제하면, 평문이던 데이터베이스가
  **자동으로 암호화 형식(SQLCipher)으로 1회 변환**됩니다. 변환은 즉시 끝나며 별도
  조작이 필요 없습니다.

### ⚠️ 업데이트 전 백업 권장
자동 변환은 데이터 파일을 교체합니다. 만일을 대비해 아래 파일을 먼저 복사해 두세요:
```
%APPDATA%\com.desknote.app\desknote.db
```
(사용자 지정 데이터 폴더를 쓰는 경우 해당 폴더의 `desknote.db`)

## 4. 보안 동작 요약

- **데이터 암호화**: `desknote.db` 전체가 SQLCipher로 암호화됩니다. 비밀번호 없이는
  파일을 열어도 내용을 볼 수 없습니다. 백업 파일도 동일하게 암호화됩니다.
- **오프라인 전용**: 웹뷰는 CSP `connect-src 'none'` 로 외부 연결이 원천 차단됩니다.
  사이드바 하단의 **"외부 통신 N건"** 배지에서 누적 외부 통신 기록을 직접 확인할 수
  있습니다(기본값 0건).
- **노션 연동**: 기본 비활성. **설정 → 외부 연동**에서 직접 켜고 업로드를 실행할
  때에만 `api.notion.com` 으로 통신하며, 그 외에는 어떤 데이터도 외부로 나가지
  않습니다.
- **자동 잠금**: 설정에서 지정한 시간 동안 활동이 없으면 잠기며, 이때 복호화 키가
  메모리에서 제거됩니다.

## 5. 제거

Windows **설정 → 앱 → 설치된 앱**에서 D-Note 를 제거합니다. 데이터 폴더
(`%APPDATA%\com.desknote.app`)는 남으므로, 완전히 지우려면 수동으로 삭제하세요.
