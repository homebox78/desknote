# D-Note 사용 설명서 (User Manual)

오프라인 전용 · 암호화 데스크톱 노트 / 문서 / 데이터베이스 앱
**버전 1.2.0 (SQLCipher 암호화)** · Windows x64

이 문서는 설치부터 모든 기능 사용법까지 단계별로 설명합니다.

---

## 목차
1. [D-Note란](#1-d-note란)
2. [다운로드 & 설치](#2-다운로드--설치)
3. [첫 실행 — 비밀번호 설정 / 업데이트](#3-첫-실행)
4. [화면 구성 한눈에 보기](#4-화면-구성)
5. [페이지 관리 (만들기·트리·즐겨찾기·휴지통)](#5-페이지-관리)
6. [글쓰기 에디터 사용법](#6-글쓰기-에디터-사용법)
7. [내보내기 (Markdown·HTML·PDF)](#7-내보내기)
8. [버전 기록 (자동 스냅샷·복원)](#8-버전-기록)
9. [데이터베이스 (표·보드·갤러리·캘린더)](#9-데이터베이스)
10. [포스트잇 (바탕화면 메모)](#10-포스트잇)
11. [검색](#11-검색)
12. [가져오기 & 탐색기 "D-Note로 보내기"](#12-가져오기)
13. [노션 연동](#13-노션-연동)
14. [설정 전체 안내](#14-설정-전체-안내)
15. [보안과 외부 통신 배지](#15-보안과-외부-통신-배지)
16. [데이터 위치 · 백업 · 복구](#16-데이터-위치--백업--복구)
17. [단축키 모음](#17-단축키-모음)
18. [문제 해결 (FAQ)](#18-문제-해결-faq)
19. [제거](#19-제거)

---

## 1. D-Note란

D-Note는 노션(Notion) 스타일의 노트·문서·데이터베이스 앱이지만, **모든 데이터가
사용자 PC를 절대 벗어나지 않는** 것을 제1원칙으로 만든 오프라인 전용 앱입니다.

- **로컬 저장 + 전체 암호화**: 모든 내용은 암호화된 데이터베이스 파일 하나에 저장됩니다.
- **네트워크 차단**: 앱은 외부로 통신하지 않습니다(업데이트 확인·광고·추적 없음).
- **유일한 예외**: 사용자가 설정에서 직접 켜고 실행하는 **노션 업로드**뿐이며, 그조차
  사이드바 하단 기록에 그대로 남습니다.

---

## 2. 다운로드 & 설치

### 다운로드
- **릴리스 페이지**: https://github.com/homebox78/desknote/releases
- **설치 파일 바로 받기**:
  [D-Note_1.2.0_x64-setup.exe](https://github.com/homebox78/desknote/releases/download/v1.2.0-sqlcipher/D-Note_1.2.0_x64-setup.exe)

### 설치 절차
1. 받은 `D-Note_1.2.0_x64-setup.exe` 를 **더블클릭**합니다.
2. **Windows SmartScreen 경고**가 나타나면(서명되지 않은 설치 파일이라 정상입니다)
   → **추가 정보** → **실행**을 누릅니다.
3. **관리자 권한(UAC)** 허용을 묻습니다 → **예**. (시스템 전체 설치라 필요합니다.)
4. **사용권 계약(EULA)** 에 동의하고 안내에 따라 진행합니다.
5. 설치가 끝나면 **시작 메뉴** 또는 **바탕화면**의 **D-Note** 아이콘으로 실행합니다.

> **WebView2 런타임**: 앱 구동에 필요합니다. 없는 PC라면 설치 중 자동으로 내려받습니다
> (이 단계에만 인터넷 필요). 최신 Windows 10/11에는 대부분 기본 포함되어 있습니다.

---

## 3. 첫 실행

### 처음 사용하는 경우 — 온보딩 4단계
1. **환영** 화면 → 다음
2. **비밀번호 설정** — 마스터 비밀번호(8자 이상 권장)를 입력하고 한 번 더 확인합니다.
   입력하는 동안 강도(약함/보통/강함)가 표시됩니다.
3. **모양** — 테마(라이트/다크)와 강조 톤을 미리 고를 수 있습니다.
4. **완료** → 워크스페이스로 진입합니다.

> ⚠️ **마스터 비밀번호는 분실 시 복구가 불가능합니다.** 이 비밀번호로 데이터 전체가
> 암호화되므로, 잊어버리면 어떤 방법으로도 내용을 열 수 없습니다. 안전하게 보관하세요.

### 이미 쓰던 사용자 — 자동 암호화 변환
기존 버전을 쓰다가 이 버전으로 업데이트한 경우, **기존 비밀번호로 잠금을 해제하면**
평문이던 데이터베이스가 **자동으로 암호화 형식(SQLCipher)으로 1회 변환**됩니다.
변환은 즉시 끝나며 별도 조작이 필요 없습니다. (변환 전 백업 권장 → [16장](#16-데이터-위치--백업--복구))

### 잠금 해제
앱을 켤 때마다, 또는 자동 잠금 후에는 마스터 비밀번호를 입력해 잠금을 해제합니다.
비밀번호가 틀리면 데이터베이스 자체가 열리지 않습니다(앱 진입만 막는 게 아니라
데이터 접근이 차단됩니다).

---

## 4. 화면 구성

```
┌───────────────────────────────────────────────┐
│  타이틀바   제목            ⚙️설정 📌항상위 ─ □ ✕ │
├───────────┬───────────────────────────────────┤
│ 사이드바   │  본문 영역                          │
│  D-Note   │   📄 [아이콘] 제목                   │
│  🔍 검색   │   ┌─ 내보내기바: MD HTML PDF 기록 포스트잇 │
│  🗑 휴지통 │   │                                 │
│  ★즐겨찾기 │   │   여기에 글을 씁니다 (블록 에디터)   │
│  개인 페이지 │   │                                 │
│   • 페이지들│   │                                 │
│  ＋새 페이지 │   │                                 │
│  🗃 새 DB   │   │                                 │
│  📝 새 포스트잇│  │                                 │
│  ⬇ 가져오기 │   │                                 │
│  ⬆ 노션 ZIP │   │                                 │
│  ────────  │   │                                 │
│  🛡 외부통신 0건│ │                                 │
└───────────┴───────────────────────────────────┘
```

- **타이틀바**(상단): 현재 페이지 제목, 우측에 **설정 ⚙️**, **항상 위 📌**,
  최소화 / 최대화 / 닫기 버튼.
- **사이드바**(좌측): 검색·휴지통, 즐겨찾기, 페이지 트리, 만들기 버튼들, 가져오기/노션,
  맨 아래 **외부 통신 기록 배지**.
- **본문**(우측): 선택한 페이지의 제목·아이콘과 에디터(또는 데이터베이스 뷰).

---

## 5. 페이지 관리

### 페이지 만들기
- 사이드바 **`＋ 새 페이지`** — 일반 문서 페이지
- 사이드바 **`🗃 새 데이터베이스`** — 표/보드/갤러리/캘린더 페이지
- 트리에서 페이지에 마우스를 올리면 나타나는 **`＋`** — 그 페이지의 **하위 페이지** 생성

### 페이지 트리 (무한 중첩)
- 페이지 안에 페이지를 무한히 중첩할 수 있습니다.
- 화살표(▸/▾)로 하위 페이지를 펼치거나 접습니다.
- 페이지를 클릭하면 본문에 열립니다.

### 제목과 아이콘
- 본문 상단의 **제목 칸**에 입력하면 즉시 반영됩니다(Enter 또는 다른 곳 클릭 시 저장).
- 제목 왼쪽 **아이콘**을 클릭 → 이모지를 입력해 페이지 아이콘을 바꿀 수 있습니다.

### 즐겨찾기 ★
- 트리에서 페이지에 마우스를 올리면 오른쪽에 **별(★)** 이 나타납니다. 클릭하면
  즐겨찾기에 등록되고, 사이드바 상단 **"즐겨찾기"** 섹션에 고정됩니다.
- 별을 다시 누르면 해제됩니다.

### 우클릭 메뉴 (페이지)
트리의 페이지를 **우클릭**하면:
- **하위 페이지 추가** ＋
- **하위 데이터베이스 추가** 🗃
- **포스트잇으로 열기** 📌
- **삭제** 🗑 (휴지통으로 이동, 하위 페이지도 함께)

### 휴지통 (복구 / 영구 삭제)
- 사이드바 **`🗑 휴지통`** → 삭제한 페이지 목록.
- 각 항목에서 **복원**(되살리기) 또는 **영구 삭제**(완전 제거)를 선택합니다.
- 삭제는 우선 휴지통으로 가는 **소프트 삭제**라, 실수로 지워도 복구할 수 있습니다.

---

## 6. 글쓰기 에디터 사용법

D-Note의 에디터는 **블록 단위**로 동작합니다(문단·제목·목록·인용·코드 등이 각각 블록).

### 글 입력과 자동 저장
- 본문을 클릭하고 바로 타이핑하면 됩니다.
- **자동 저장**: 입력을 멈추면 약 0.6초 뒤 자동 저장됩니다(따로 저장 버튼 없음).

### 슬래시 `/` 메뉴 — 블록 삽입
빈 줄에서 **`/`** 를 입력하면 삽입 메뉴가 뜹니다. 제목, 목록, 체크박스, 표, 코드,
이미지 등 다양한 블록을 골라 넣을 수 있습니다. 이름 일부를 타이핑해 빠르게 찾으세요.

### 마크다운 단축 입력
줄 맨 앞에서 아래를 입력하고 스페이스를 누르면 자동 변환됩니다:
- `# ` → 제목 1, `## ` → 제목 2, `### ` → 제목 3
- `- ` 또는 `* ` → 글머리 기호 목록
- `1. ` → 번호 목록
- `> ` → 인용구
- ` ``` ` → 코드 블록

### 서식 (굵게·기울임 등)
- 글자를 **드래그 선택**하면 떠오르는 도구막대에서 굵게/기울임/밑줄/링크 등을 적용.
- 또는 본문을 **우클릭** → 아래 블록 메뉴의 서식 버튼 사용.

### 우클릭 블록 메뉴 (핵심 기능)
본문을 **우클릭**하면 D-Note 전용 블록 메뉴가 나타납니다:

| 구역 | 기능 |
| --- | --- |
| **서식** | **B**(굵게) · *I*(기울임) · ~~S~~(취소선) · `</>`(인라인 코드) — 현재 적용 상태가 강조 표시 |
| **블록 전환** | 텍스트 · 제목 1 · 제목 2 · 글머리 기호 목록 · 인용 · 코드 (현재 종류에 ✓ 체크) |
| **블록 동작** | 아래에 블록 추가 ＋ · 복제 · 삭제 🗑 |
| **첨부** | 이미지 첨부 · 파일 첨부 |

### 이미지 / 파일 첨부
- 우클릭 메뉴 → **이미지 첨부** / **파일 첨부** 로 로컬 파일을 선택하면, 파일이
  앱의 `assets` 폴더로 **복사 저장**되고 커서 위치에 삽입됩니다.
- 파일을 에디터로 **드래그 앤 드롭**해도 첨부됩니다.
- 모든 첨부는 로컬에만 저장되며 외부로 전송되지 않습니다.

### 내보내기 바
본문 상단의 칩 버튼 줄에서 **Markdown · HTML · PDF** 내보내기, **기록**(버전),
**포스트잇**(현재 페이지를 메모 창으로) 기능에 바로 접근합니다.

---

## 7. 내보내기

본문 상단 내보내기 바에서:
- **Markdown** — `.md` 텍스트 파일
- **HTML** — 서식이 보존된 `.html`
- **PDF** — 인쇄용 `.pdf`

저장 위치를 묻는 창이 뜨면 경로를 지정합니다.

> ⚠️ 내보낸 파일은 **암호화되지 않은 평문**입니다. 암호화 영역(앱 내부) 밖으로
> 데이터를 꺼내는 것이므로 보관에 유의하세요.
> ⚠️ **PDF 한글**: 기본 폰트가 라틴 전용이라 한글이 깨질 수 있습니다(HTML·Markdown은
> 정상). 한글 문서는 HTML 내보내기를 권장합니다.

---

## 8. 버전 기록

- 페이지를 저장할 때마다 자동으로 **버전 스냅샷**이 남습니다(약 3분 간격, 최근 50개 유지).
- 내보내기 바의 **`🕘 기록`** 버튼 → 이전 버전 목록에서 시점을 골라 **복원**할 수 있습니다.
- 복원하면 현재 내용이 그 시점으로 되돌아갑니다(되돌리기 전 내용도 다시 버전으로 남음).

---

## 9. 데이터베이스

데이터베이스 페이지는 표 형태의 구조화된 데이터를 4가지 방식으로 봅니다.

### 뷰 전환
상단 탭에서 전환:
- **표(Table)** — 행과 열의 기본 표
- **보드(Board)** — 칸반 보드(선택 속성 기준 그룹, 카드 드래그로 이동)
- **갤러리(Gallery)** — 카드형, **커버 이미지** 첨부 가능
- **캘린더(Calendar)** — 날짜 속성 기준, 날짜 클릭 시 항목 추가

### 속성(열) 종류
텍스트 · 숫자 · 선택(단일) · 다중 선택 · 날짜 · 체크박스 · URL.
선택 속성은 입력하면서 **옵션을 즉석에서 생성**할 수 있습니다.

### 행 편집
- 표에서 **셀을 클릭**해 바로 편집합니다.
- 카드를 클릭하면 **행 상세 모달**이 열려 모든 속성을 한 번에 편집할 수 있습니다.
- 보드에서는 추가 시 해당 그룹 값에 맞춰 행이 생성됩니다.

---

## 10. 포스트잇 (바탕화면 메모)

페이지를 **항상 위에 떠 있는 작은 메모 창**으로 띄우는 기능입니다.

### 여는 방법
- 사이드바 **`📝 새 포스트잇`**
- 본문 상단 내보내기 바의 **`포스트잇`** 칩(현재 페이지를 띄움)
- 페이지 우클릭 → **포스트잇으로 열기**

### 특징
- **페이지 미러**: 포스트잇과 원본 페이지의 내용이 완전히 연동됩니다(한쪽 수정 → 양쪽 반영).
- **색상 팔레트**: 노랑·핑크·블루·그린·그레이.
- 드래그로 이동, 크기 조절 가능. **위치·크기·색이 저장**됩니다.
- 상단에 **"동기화 중… / 동기화됨"** 표시.
- **"페이지 열기"** 를 누르면 메인 창에서 해당 페이지가 열립니다.
- 앱을 재시작하면 열어두었던 포스트잇이 **자동 복원**됩니다.

---

## 11. 검색

- 사이드바 **`🔍 검색`** 또는 단축키 **⌘/Ctrl + K**.
- 제목과 본문을 **전문 검색(FTS5)** 합니다.
- 결과 목록에서 **↑ ↓** 로 이동, **Enter** 로 해당 페이지 열기.

---

## 12. 가져오기

### 파일 가져오기
사이드바 **`⬇ 가져오기 (MD/DOCX/CSV)`** → 파일 선택:
- **Markdown(.md)**, **Word(.docx)** → 새 문서 페이지로
- **CSV**, **Excel(.xlsx)** → 새 데이터베이스 페이지로

### 탐색기 "D-Note로 보내기"
- Windows 파일 탐색기에서 문서 파일을 **우클릭** → **"D-Note로 보내기"** 로 바로 가져옵니다.
- 이 메뉴는 앱을 한 번 실행해 잠금을 해제하면 자동 등록됩니다(관리자 권한 불필요).

---

## 13. 노션 연동

D-Note는 노션으로 데이터를 옮기는 두 가지 방법을 제공합니다.

### ① 노션 가져오기용 ZIP 내보내기 (오프라인, 권장)
- 사이드바 **`⬆ 노션으로 내보내기 (ZIP)`** → 중첩 Markdown + CSV로 묶인 ZIP 생성.
- 노션에서 **설정 → 가져오기 → Markdown & CSV** 로 이 ZIP을 업로드합니다.
- **완전히 오프라인**입니다(파일만 만들 뿐 외부 통신 없음).

### ② 노션 API 직접 업로드 (기본 OFF · 옵트인)
이 기능은 외부(`api.notion.com`)로 데이터를 전송하므로 **기본적으로 꺼져 있고
사이드바에 보이지 않습니다.** 켜는 방법:

1. **설정 → 외부 연동** 으로 이동.
2. **"노션에 직접 업로드 (API)"** 토글을 켭니다 → 외부 전송 경고 확인창에 동의.
3. 같은 카드에 나타나는 **"업로드 실행"** 버튼을 누릅니다.
4. 노션 **통합 토큰**과 **대상(부모) 페이지 URL/ID** 를 입력해 업로드합니다.

> 직접 켜고 실행하지 않는 한, 어떤 데이터도 외부로 나가지 않습니다. 업로드한 모든
> 요청은 사이드바 하단 **외부 통신 기록**에 남습니다([15장](#15-보안과-외부-통신-배지)).
> 참고: DB 페이지는 업로드에서 건너뜁니다(CSV로 이전), 이미지는 로컬 파일이라 제외됩니다.

---

## 14. 설정 전체 안내

우측 상단 **⚙️** 로 설정 페이지를 엽니다. 카드별 항목:

### 모양
| 항목 | 설명 |
| --- | --- |
| 테마 | 라이트 / 다크 |
| 강조 톤 | 버튼·강조색(무채색 잉크) — 잉크/그래파이트/스틸 |
| 사이드바 밀도 | 편안함 / 컴팩트 (행 간격·글자 크기) |
| 사이드바 너비 | 216–328px (8px 단위) |
| 전체폭 본문 | 문서 본문을 넓게 펼침 (끄면 가운데 정렬·읽기 폭) |
| 본문 글자 크기 | 90–120% (5% 단위) |

### 보안
| 항목 | 설명 |
| --- | --- |
| 마스터 비밀번호 | Argon2id 키 파생 · SQLCipher로 DB 전체 암호화 (변경 기능은 준비 중) |
| 자동 잠금 | 끔 / 5분 / 15분 / 30분 — 비활성 시 자동 잠금(키를 메모리에서 제거) |
| 네트워크 | 차단됨 · CSP `connect-src 'none'` (상태 표시) |

### 외부 연동
| 항목 | 설명 |
| --- | --- |
| 노션에 직접 업로드 (API) | 기본 OFF. 켜야만 사이드바가 아닌 이 설정에서 업로드 가능 |
| 지금 업로드 | (켰을 때만) 토큰·대상 페이지 입력해 전송 |

상단에 **"직접 켜고 실행하지 않는 한 데이터는 외부로 나가지 않는다"** 는 공지가 표시됩니다.

### 백업
| 항목 | 설명 |
| --- | --- |
| 자동 백업 | 하루 1회 날짜별 DB 스냅샷 (기본 켜짐) |
| 백업 위치 | `…\backups` 폴더 — **열기** 버튼으로 탐색기에서 열기 |
| 마지막 백업 | 마지막 백업 날짜 표시 |

### 정보
| 항목 | 설명 |
| --- | --- |
| 버전 / 빌드 | D-Note 1.2.0 · Tauri 2 · React 19 · SQLite |
| 데이터 위치 | 현재 데이터 폴더 경로 · **변경** 버튼으로 다른 폴더로 이전(재시작 필요) |

---

## 15. 보안과 외부 통신 배지

D-Note는 "안전하다"고 주장하는 대신 **눈으로 확인**할 수 있게 만들었습니다.

### 외부 통신 기록 배지
사이드바 맨 아래:
- **🛡 외부 통신 0건 · 차단됨** (녹색) — 지금까지 단 한 건의 외부 통신도 없음.
- 노션 업로드를 실행하면 **🛡 외부 통신 N건** (주황)으로 바뀝니다.
- 배지를 클릭하면 **전체 기록 모달**이 열려, 언제 어떤 호스트로 어떤 요청이 나갔는지
  모두 확인할 수 있습니다.

### 보안 계층 요약
1. **데이터 암호화** — `desknote.db` 전체가 SQLCipher로 암호화. 비밀번호 없이는 파일을
   열어도 내용을 볼 수 없습니다. 백업 파일도 동일하게 암호화됩니다.
2. **네트워크 차단** — 웹뷰는 CSP `connect-src 'none'` 로 외부 연결 원천 차단.
3. **키 파생** — 마스터 비밀번호 → Argon2id → 암호화 키.
4. **자동 잠금** — 잠길 때 복호화 키를 메모리에서 제거.

---

## 16. 데이터 위치 · 백업 · 복구

### 데이터 폴더 (Windows)
```
%APPDATA%\com.desknote.app\
```
> 표시 이름은 D-Note지만 내부 식별자/파일명은 `desknote`를 유지합니다(기존 데이터 호환).
> 폴더 경로가 `com.desknote.app` 인 것은 정상입니다.

| 파일/폴더 | 내용 |
| --- | --- |
| `desknote.db` | 모든 페이지·본문·데이터베이스 (**SQLCipher 암호화**) |
| `vault.hold` | 비밀번호 검증용 금고 (Stronghold) |
| `assets/` | 첨부한 이미지·파일 |
| `backups/` | 날짜별 DB 스냅샷 |
| `network-log.json` | 외부 통신 기록(투명성 로그) |

### 수동 백업 / 복구
- **백업**: 위 폴더 전체(특히 `desknote.db`)를 복사해 두면 됩니다.
- **복구**: 같은 비밀번호를 쓰는 환경에서 `desknote.db` 를 같은 위치에 덮어쓰면 됩니다.

### ⚠️ 업데이트 시 주의
기존 평문 DB → 암호화 변환은 파일을 교체합니다. 업데이트 전 `desknote.db` 를 먼저
복사해 두면, 문제가 생겨도 이전 버전으로 되돌릴 수 있습니다.

---

## 17. 단축키 모음

| 단축키 | 동작 |
| --- | --- |
| **⌘/Ctrl + K** | 검색 열기 |
| **↑ / ↓** | (검색 결과) 이동 |
| **Enter** | (검색) 페이지 열기 / (제목) 입력 확정 |
| **/** | (에디터) 블록 삽입 메뉴 |
| **우클릭** | (에디터) 블록 메뉴(서식·전환·첨부) |
| `#`, `-`, `>`, ` ``` ` + 스페이스 | 마크다운 단축 변환 |
| **Ctrl + R** | (개발 모드 한정) 새로고침 |

---

## 18. 문제 해결 (FAQ)

**Q. 비밀번호를 잊어버렸어요.**
A. 복구할 수 없습니다. 데이터가 그 비밀번호로 암호화되어 있어 의도된 동작입니다.
정기적으로 비밀번호를 안전한 곳에 보관하세요.

**Q. 설치 시 "Windows의 PC 보호" 경고가 떠요.**
A. 코드 서명이 없는 설치 파일이라 나타나는 정상 경고입니다. **추가 정보 → 실행**.

**Q. 업데이트 후 첫 실행이 평소보다 느려요.**
A. 기존 평문 DB를 암호화본으로 1회 변환하기 때문입니다. 이후 실행은 정상 속도입니다.

**Q. PDF로 내보냈더니 한글이 깨져요.**
A. PDF 기본 폰트가 라틴 전용입니다. 한글 문서는 **HTML 내보내기**를 사용하세요.

**Q. 노션 업로드 메뉴가 사이드바에 없어요.**
A. 기본적으로 꺼져 있습니다. **설정 → 외부 연동**에서 켜면 거기서 업로드할 수 있습니다.

**Q. 정말 외부로 아무것도 안 나가나요?**
A. 웹뷰는 CSP로 외부 연결이 차단되어 있고, 유일한 예외인 노션 업로드도 옵트인이며
사이드바 하단 **외부 통신 기록**에 모두 남습니다. 0건이면 0건이 사실입니다.

**Q. 데이터를 다른 폴더(예: 외장 드라이브)에 두고 싶어요.**
A. **설정 → 정보 → 데이터 위치 → 변경** 에서 폴더를 지정하면 됩니다(재시작 필요).

---

## 19. 제거

Windows **설정 → 앱 → 설치된 앱** 에서 **D-Note** 를 제거합니다.
데이터 폴더(`%APPDATA%\com.desknote.app`)는 남으므로, 데이터까지 완전히 지우려면
해당 폴더를 수동으로 삭제하세요.

---

*이 설명서는 D-Note 1.2.0 (SQLCipher 암호화) 기준입니다.*
